{
  "name": "mobot_api",
  "version": "1.0.0",
  "description": "The api for MoBot - Your moodle tracking bot",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node dist/app.js",
    "scrape": "npm run build && node dist/local.js"
  },
  "author": "Pranay Agarwal",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@types/node": "^16.9.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  }
}
